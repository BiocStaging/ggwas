% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-format.R
\name{as_granges}
\alias{as_granges}
\title{Convert GWAS results to a GRanges object}
\usage{
as_granges(x, ...)
}
\arguments{
\item{x}{A \code{gwas_data} object or a data.frame accepted by \code{\link[=as_gwas_data]{as_gwas_data()}}.}

\item{...}{Column overrides passed to \code{\link[=as_gwas_data]{as_gwas_data()}} when \code{x} is a plain
data.frame.}
}
\value{
A \link[GenomicRanges]{GRanges} object, one width-1 range per
variant, named by SNP when available.
}
\description{
Turn a \code{gwas_data} object or data.frame into a Bioconductor
\link[GenomicRanges]{GRanges} object, with association statistics stored as
metadata columns, for interoperability with the Bioconductor ecosystem
(e.g. VariantAnnotation, SummarizedExperiment, coloc).
}
\examples{
data(example_gwas)
gr <- as_granges(example_gwas)
gr

# Round-trip back to gwas_data
as_gwas_data(gr)
}
